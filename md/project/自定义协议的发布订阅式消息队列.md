# 自定义协议的发布订阅式消息队列

⾃定义协议的发布订阅式消息队列通⽤问题

### 1.为什么做这个项⽬？

在学习过程中,接触到了rabbitmq,就很好奇rabbitmq是怎么实现的.翻了下rabbitmq的源码,发现是erlang语⾔实现的,erlang的语法⻛格和Java差异很⼤(函数式编程,使⽤模式匹配代替分⽀语句,
使⽤递归代替循环语句)因此就想仿照它的核⼼功能,尝试⾃⼰实现了下.
MQ这个主题⼀般不会作为学校的课设.同学们回答的时候建议还是从兴趣⻆度切⼊.

### 2.这个项⽬为啥和上个(之前)同学的项⽬⼀样？

因为这个项⽬本⾝在实现的过程中，在⽹上也搜索了⼀些相关的博客分析，借鉴了⼀些开源项⽬，写得优质的主要就是那⼏篇，可能我们参考了⼀样的博客讲解梳理框架吧
（注：以下内容需要同学⾃⼰思考项⽬扩展，如不清楚，不建议参照话术，容易给⾃⼰挖坑。说到上
⾯的内容就可以了。不要使⽤课上讲解的扩展，⼤家都说⼀样的也不⾏）
但这个项⽬除了常规流程，我还进⾏了（功能扩展/性能优化/代码优化）.....(并解释⼀下扩展点)

### 3.介绍⼀下这个项⽬。包括⼀些核⼼概念及⼯作流程参考：RabbitMQ的⼯作流程

RabbitMQ是⼀个消息中间件,也是⼀个⽣产者消费者模型.它负责接收,存储并转发消息.根据⼯作流程图,来介绍它的核⼼概念:
•Producer:⽣产者,向RabbitMQ发送消息
•Consumer:消费者,从RabbitMQ接收消息

•消费者,从 Q接收消
•Broker:消息队列服务器或服务实例,也就是RabbitMQServer
•Connection:⽹络连接,它允许客⼾端与RabbitMQ通信
•Channel:连接⾥的⼀个虚拟通道,发送或者接收消息都是通过通道进⾏的.
•Exchange:交换机.负责接收⽣产者发送的消息,并根据路由算法和规则将消息路由到⼀个或多个队列
•Queue:消息队列,存储消息直到它们被消费者消费
•Binding:交换机和队列之间的映射关系
⼯作流程:

### 1.创建连接:Producer连接到Broker,建⽴⼀个连接(Connection),开启⼀个信道(Channel)


### 2.声明交换机和队列,以及绑定规则:Producer声明⼀个交换机(Exchange)和队列,并绑定Queue到

Exchange

### 3.发布消息:Producer发送消息⾄Broker

4.消息存储:Broker接收消息,并存⼊相应的队列(Queue)中,如果未找到相应的队列,则根据⽣产者的配置,选择丢弃或者退回给⽣产者.

### 5.消费消息:消费者监听Queue,当消息到达时,从Queue中获取消息,处理后,向RabbitMQ发送消息确认


### 6.消息删除:消息被确认后,Broker会把消息从Queue中删除.


### 4.项⽬最终的成果如何？你从这个项⽬中学到了什么？

•实现了消息队列的核⼼功能.后续开发⼀些其他项⽬就可以考虑使⽤我⾃⼰的消息队列替换
rabbitmq
•最主要的收获是通过这个项⽬,更深刻的理解了消息队列的使⽤⽅法和⼯作原理.
•这个项⽬中基于Socket进⾏⽹络通信,使我对于⽹络的底层原理有更清楚的认知,尤其是⾃⼰设计应⽤层协议.
•学到了当项⽬中遇到问题，迫使我思考和寻找解决⽅案，这增强了我的问题解决能⼒。
•学到了如何进⾏有效的测试，以确保项⽬不出现bug。

### 5.你在这个项⽬中使⽤了哪些技术？为什么选择(决策)这些技术？

•Spring:Java后端主流技术
•Socket&⾃定义应⽤层协议:希望通过使⽤Socket加深对于⽹络底层操作/TCP协议的理解
•数据结构/集合类:组织数据必要组件

•各种流对象(读写⽹络,读写⽂件):⽹络通信和持久化消息都需要进⾏IO操作
•SQLite数据库:rabbitmq在保存元数据时使⽤了erlang⽣态中的Mnesia数据库.此处使⽤mysql
存储太重量了,后续部署会⽐较⿇烦.因此直接使⽤sqlite轻量的数据库更好的选择.(本⾝存储元数据,数据量也不多,数据结构也⽐较简单).对于⼤量的数据(⽐如消息数据采取⽂件的⽅式来存储)
Mnesia在英语中的发⾳是m ɪ ˈni ːzi əm ɪ ˈni ːzi ə。发⾳时，"m"发⾳类似于"m"，"n"发⾳类似于
"n"，"i"发⾳类似于"ee"（如"meet"），"z"发⾳类似于"z"。所以，它听起来像"mih-NEE-zee-
uh"。
•Unittest:spring内置单元测试⼯具.针对关键功能进⾏单元测试.

### 6.你在项⽬中扮演了什么⻆⾊？你负责了哪些模块或功能？

独⽴完成项⽬的设计,开发,测试,部署

### 7.在项⽬中，你是如何平衡技术实现和项⽬时间线的？这个项⽬做了多⻓时间？

这个项⽬我花了2个⽉来完成，从项⽬⽬标的确定，到项⽬原型图的敲定，到前后端功能的拆分，在到最后的开发测试部署。
关于时间，其实我没有制定详细的规划。是因为我也是抱着学习的⼼态来完成这个项⽬。在开发中可能会遇到⼀些问题，解决问题也是我学习的过程，因此时间就不能确定下来。
回复思路可参考：

### 1.明确项⽬范围和⽬标：在项⽬开始之前，我确定了项⽬到底要做什么，对范围、⽬标和期望有清晰的认知。


### 2.制定详细的计划（如果有）：创建⼀个详细的项⽬计划，包括项⽬核⼼功能敲定，到模块划分，

前后端交互接⼝设计。

### 3.优先级排序：确定哪些功能和特性是当下最重要的，并根据优先级进⾏排序。这有助于在时间有限的情况下做出决策。

4.⼈员分配（如果有）：由于前端是交付给其他同学完成，以上内容也需要和前端同学对⻬。（如果说前端也是⾃⼰完成的，就不⽤说这⼀点）本项⽬不涉及前端,其他项⽬可以参考.

### 5.沟通和协作（如果有）：确保和前端同学有良好的沟通和协作，以便快速解决问题并共享信息。

（如果说前端也是⾃⼰完成的，就不⽤说这⼀点）

### 6.质量保证：计划中还包含⾜够的时间来测试项⽬，进⾏质量保证。


### 8.你在项⽬中遇到了哪些挑战？你是如何识别和解决这些问题的？


•在调研阶段,阅读rabbitmq的源码,挑战较⼤.Rabbitmq的代码是erlang语⾔开发,语法⻛格和
java差异较⼤(使⽤模式匹配代替条件语句,使⽤递归代替循环语句).后来通过⽹上搜集资料,了解
rabbitmq的各种核⼼特性,尝试⾃⼰对照功能来进⾏实现
•开发阶段针对客⼾端服务器之间的通信⽅式的实现,由于决定采⽤tcpsocket,就需要⼿动实现⼀
些连接管理,⾃定义协议处理粘包问题,⽀持数据报⽂和控制报⽂等细节逻辑.这⾥是花了较多的时间设计开发的.
•测试阶段遇到了xxbug,经过⼀系列的定位过程,最终找到了问题,进⾏了修复.
上述只是提供⼀个切⼊的思路.具体如何回答,每个同学要结合⾃⼰做项⽬过程中的感受来组织语⾔.
⾯试官问这样的问题本质上就是试探同学是否是真的把项⽬做出来了,还是抄来的.
切忌凭空编造!很容易露馅!

### 9.你是如何测试和部署项⽬的？

测试：

### 1.单元测试：编写测试代码来验证每个独⽴模块或函数的正确性。


### 2.集成与功能测试：在单元测试之后，测试各个模块如何⼀起⼯作，以及功能接⼝是否正确返回。


### 3.⽇志打印：使⽤⽇志打印来记录、分类和修复发现的问题。

部署：

### 1.构建⾃动化：通过Maven来编译代码和⽣成部署包。


### 2.环境准备：确保部署环境（开发、⽣产）都已正确设置。


### 3.部署：将包发布在阿⾥云服务器并后台启动


### 4.⽇志：部署后收集⽇志以便于问题诊断。


### 10.你是如何优化项⽬性能的？你有哪些性能调优的经验？

•⽐如在⽹络通信这⾥,使⽤⼆进制协议代替json这样的⽂本协议,能够缩减传输数据量,节省⽹络通信带宽.
•⽐如在管理数据这⾥,通过选择合适的数据结构(⽐如hashmap),优化数据访问的效率
•另外,还可以对项⽬进⾏压测,通过添加⽇志,记录各个关键环节的时间戳,找到性能瓶颈,再做针对性的优化.

### 11.你是如何设计项⽬以⽀持未来的扩展？你是如何确保代码的可维护性的？


设计应⽤层协议阶段,就需要对协议的扩展性通⽤性进⾏考虑.
此处核⼼设计思路是"操作码"+"操作参数"这样的模式,后续如果需要扩展更多的功能,协议格式整体不需要变动,只需要添加新的操作码,并且针对性的添加操作参数即可.
专属问题

### 1.你了解过其他⼀些消息队列吗？你的消息队列相较于其他的有何区别或优势？

类似问题:
了解过哪些MQ,与其他同类产品的对⽐
kafka和RabbitMQ的对⽐
对⽐其他消息队列，不同mq分别⽤在什么场景
kafka和rocketmq⽐较消息队列除了使⽤RabbitMQ,可以⽤RocketMQ吗?
⽬前业界有很多的MQ产品,例如RabbitMQ,RocketMQ,ActiveMQ,Kafka,ZeroMQ等,
简单介绍其中3种:

### 1.Kafaka

Kafka⼀开始的⽬的就是⽤于⽇志收集和传输，追求⾼吞吐量,性能卓越,单机吞吐达到⼗万级,在⽇志领域⽐较成熟,功能较为简单,主要⽀持简单的MQ功能.适合⼤数据处理,⽇志聚合,实时分析等场景

### 2.RabbitMQ

采⽤Erlang语⾔开发,MQ功能⽐较完备,且⼏乎⽀持所有主流语⾔,开源提供的界⾯也⾮常友好,性能较好,吞吐量能达到万级,社区活跃度较⾼，⽂档更新频繁,⽐较适合中⼩型公司,数据量没那么⼤,且并发没那么⾼的场景.

### 3.RocketMQ

采⽤Java语⾔开发,由阿⾥巴巴开源,后捐赠给了Apache.在可⽤性,可靠性以及稳定性等⽅⾯都⾮常出
⾊,吞吐量能达到⼗万级,在Alibaba集团内部⼴泛使⽤,但⽀持的客⼾端语⾔不多,产品较新⽂档较少,
且社区活跃度⼀般.适合于⼤规模分布式系统,可靠性要求⾼,且并发⼤的场景,⽐如互联⽹⾦融.
这些消息队列,各有侧重,没有好坏,只有适合不适合,在实际选型时,需要结合⾃⾝需求以及MQ产品特征,综合考虑我⾃⼰的消息队列只是为了满⾜⾃⼰的好奇⼼,从优势上来讲肯定是不如⼈家成熟的商业产品的.


### 2.这个项⽬借鉴了哪个消息队列？为什么选择这个？

借鉴了RabbitMQ
RocketMQ的核⼼概念设定和RabbitMQ⼤同⼩异.
Kafka部署起来⽐较⿇烦(依赖zookeeper,并且需要多节点部署),对我们低配置的阿⾥云学⽣机不友好,学习起来不像RabbitMQ这么⽅便因此在学习阶段主要学习的RabbitMQ,所以在做这个项⽬的时候也就是主要参考RabbitMQ的设定.

### 3.MQ的作⽤及应⽤场景类似问题:

项⽬什么场景下使⽤到了MQ,为什么需要MQ
MQ的作⽤？使⽤场景有哪些
MQ的主要应⽤场景消息队列解耦应⽤程序的例⼦
消息队列的应⽤场景为什么说消息队列可以削峰消息队列（MQ）是⼀种应⽤程序间的通信⽅法，它允许系统组件以异步的⽅式进⾏交互,在不同的应
⽤场景下可以展现不同的作⽤,常⻅的应⽤场景如下:
•异步解耦:在业务流程中,⼀些操作可能⾮常耗时,但并不需要即时返回结果.可以借助MQ把这些操作异步化,⽐如⽤⼾注册后发送注册短信或邮件通知,可以作为异步任务处理,⽽不必等待这些操作完成后才告知⽤⼾注册成功.
•流量削峰:在访问量剧增的情况下,应⽤仍然需要继续发挥作⽤,但是是这样的突发流量并不常⻅.如果以能处理这类峰值为标准⽽投⼊资源,⽆疑是巨⼤的浪费.使⽤MQ能够使关键组件⽀撑突发访问压
⼒,不会因为突发流量⽽崩溃.⽐如秒杀或者促销活动,可以使⽤MQ来控制流量,将请求排队,然后系统根据⾃⼰的处理能⼒逐步处理这些请求.
•异步通信:在很多时候应⽤不需要⽴即处理消息,MQ提供了异步处理机制,允许应⽤把⼀些消息放⼊
MQ中,但并不⽴即处理它,在需要的时候再慢慢处理.
•消息分发:当多个系统需要对同⼀数据做出响应时,可以使⽤MQ进⾏消息分发.⽐如⽀付成功后,⽀
付系统可以向MQ发送消息,其他系统订阅该消息,⽽⽆需轮询数据库.
•延迟通知:在需要在特定时间后发送通知的场景中,可以使⽤MQ的延迟消息功能,⽐如在电⼦商务平台中，如果⽤⼾下单后⼀定时间内未⽀付，可以使⽤延迟队列在超时后⾃动取消订单
•......


### 4.项⽬中如何保证消息的可靠性？如果没有实现，说⼀说思路。

Consumer成功消费,会给broker发送⼀个应答报⽂.
应答的模式实现了⼿动应答和⾃动应答.可以根据需要选择.
•如果对于可靠性要求不⾼,但是对吞吐性能要求较⾼,就使⽤⾃动应答.
•如果对于可靠性要求较⾼,但是对吞吐性能要求不⾼,就使⽤⼿动应答.

### 5.项⽬中如何保证消息的顺序性？如果没有实现，说⼀说思路。

在项⽬中没有实现顺序性.
如果要实现,需要给消息按照顺序编号,producer每次发送的数据,编号都需要累加,consumer收到的数据按照编号在接收⽅进⾏排序参考TCP协议的实现.

### 6.项⽬中如何保证消息消费时的幂等性？如果没有实现，说⼀说思路。

幂等性⼀般是业务逻辑保证(某个业务,⽐如计费,是幂等的).对于MQ本⾝来说⼀般不提供幂等⽀持.
如果要实现的话,需要给业务请求分配唯⼀ID(不同于刚才⽹络传输的消息序号,不要求递增,往往可能是⼀个UUID类似的东西),并且在消费者这⼀端通过⼀些⽅式(数据库/redis/内存hash)记录已经处理过的请求ID.避免重复处理.

### 7.当消息过期或被拒绝，再或者队列满了，如何处理？如果没有实现，说⼀说思路。

类似问题:
你了解RabbitMQ吗？介绍下RabbitMQ的死信队列从以下⽅⾯来回答:
•死信队列的概念
•死信的来源
•死信队列的应⽤场景
1.死信队列的概念死信（DeadLetter）是消息队列中的⼀种特殊消息,它指的是那些⽆法被正常消费或处理的消息.在消息队列系统中,如RabbitMQ,死信队列⽤于存储这些死信消息


### 2.死信的来源

1)消息过期:消息在队列中存活的时间超过了设定的TTL
2)消息被拒绝:消费者在处理消息时,可能因为消息内容错误,处理逻辑异常等原因拒绝处理该消息.如果拒绝时指定不重新⼊队(requeue=false),消息也会成为死信.
3)队列满了:当队列达到最⼤⻓度,⽆法再容纳新的消息时,新来的消息会被处理为死信.
3.死信队列的应⽤场景对于RabbitMQ来说,死信队列是⼀个⾮常有⽤的特性.它可以处理异常情况下,消息不能够被消费者正确消费⽽被置⼊死信队列中的情况,应⽤程序可以通过消费这个死信队列中的内容来分析当时所遇到的异常情况,进⽽可以改善和优化系统.
⽐如:⽤⼾⽀付订单之后,⽀付系统会给订单系统返回当前订单的⽀付状态为了保证⽀付信息不丢失,需要使⽤到死信队列机制.当消息消费异常时,将消息投⼊到死信队列中,由订单系统的其他消费者来监听这个队列,并对数据进⾏处理(⽐如发送⼯单等,进⾏⼈⼯确认).
场景的应⽤场景还有:
•消息重试：将死信消息重新发送到原队列或另⼀个队列进⾏重试处理.
•消息丢弃：直接丢弃这些⽆法处理的消息，以避免它们占⽤系统资源.
•⽇志收集：将死信消息作为⽇志收集起来，⽤于后续分析和问题定位.

### 8.消息积压如何处理？什么原因可能会导致消息积压？

类似问题:
MQ消息堆积问题如何解决MQ的数据囤积？
消息积压的原因:
•消息⽣产过快
•消费者能⼒不⾜

•⽹络问题
•RabbitMQ服务配置偏低
•......
解决⽅案:
•提⾼消费者效率,⽐如增加机器,优化业务逻辑
•限制消费者⽣产速率
•资源配置优化

### 9.消息持久化后，若磁盘已满如何处理？有哪些实现策略或思路？

思路参考：
1.清理磁盘空间：检查并删除不必要的⽂件，或者移动⽂件到其他存储设备上，以释放磁盘空间。如果是⽇志⽂件占⽤了⼤量空间，可以考虑压缩或删除旧的⽇志⽂件。
2.重新分配队列数据⽂件：如果MQ因为磁盘满⽽⽆法启动，可能需要⼿动删除队列数据⽂件中可能损坏的⽂件。删除前请确保备份重要数据，因为这⼀操作会导致队列中的数据丢失。

### 3.增加磁盘容量：如果经常遇到磁盘空间不⾜的问题，可能需要考虑增加物理磁盘或调整虚拟机的磁盘配额。


### 4.监控和报警：设置监控和报警机制，以便在磁盘空间不⾜时及时收到通知并采取措施。


### 5.优化消息处理：优化消息的⽣产和消费逻辑，减少消息积压，从⽽减少磁盘上消息的存储。


### 6.使⽤不同的存储路径：如果服务器上有多个磁盘或分区，可以考虑将MQ的数据⽬录迁移到有更多可⽤空间的磁盘或分区上。


### 10.项⽬中消息传递是推模式还是拉模式？如何实现？

推模式.
消费者和broker之间建⽴了tcp⻓连接.Broker收到数据之后,就会依据订阅管理模块,找到队列对应的消费者客⼾端的socket对象,并且把队列中的数据读出来,写⼊socket,从⽽传输给对应的
consumer.
推模式实时性更好,但是在数据量⼤的时候可能对consumer造成较⼤的压⼒.
Rabbitmq默认的消费模式就是推模式.
如果要实现拉模式,只需要让broker提供tcp接⼝,让consumer主动给broker发起请求获取数据即可.